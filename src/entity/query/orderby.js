
export default class OrderBy {
  constructor(columns) {
    this.columns = columns;
    this.isDistinct = false;
  }
  execute(selectData) {
    if(Array.isArray(selectData)){
      for()
    }
    retun this;
  }
}
